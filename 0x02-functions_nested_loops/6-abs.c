#include "main.h"


/**
 * _abs - Calculates the absolute value of an interger
 * @n: The given integer to be calculated
 *
 * Return: The value
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
